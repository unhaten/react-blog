const Navbar = () => {
    return (
        <nav className="navbar">
            <h1 className="ml">The Blog</h1>
            <a href="/">Home</a>
            <a href="/create">New Blog</a>
        </nav>
    );
};

export default Navbar;
